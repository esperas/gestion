sap.ui.define([], function () {
	"use strict";

	return {
        formatButton: function (sPiece) {
            console.log(sPiece);
            if (sPiece) { return true};
            return false;

        }
	};
});
